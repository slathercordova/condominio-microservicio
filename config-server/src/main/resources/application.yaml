server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:./config-repo

#eureka
eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/